apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: nodeapp
spec:
  hosts:
  - "*"
  gateways:
  - nodeapp-gateway
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: nodeapp
        port:
          number: 3000